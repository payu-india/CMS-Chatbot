---
name: MerchantHostedCheckoutStepsHoverCards
---
---
name: MerchantHostedCheckoutStepsHoverCards
---
// MerchantHostedCheckoutStepsHoverCards - ReadMe CMS Custom Component
// Hardcoded 3-step Merchant Hosted Checkout cards. No page props.
// Name in Settings must be: MerchantHostedCheckoutStepsHoverCards
// On the page insert only: MerchantHostedCheckoutStepsHoverCards self-closing tag
// Do not add helpers above export - keep all logic inside the export.

export const MerchantHostedCheckoutStepsHoverCards = () => {
  const gapPx = 20;
  const minCardPx = 280;
  const gridTemplateColumns =
    'repeat(auto-fit, minmax(max(' +
    minCardPx +
    'px, calc((100% - ' +
    gapPx +
    'px) / 2)), 1fr))';

  const themes = [
    {
      background: 'linear-gradient(165deg, #eef3ff 0%, #e8effc 50%, #e3ebf8 100%)',
      icon: '#3b5bdb',
      iconBox: 'rgba(255,255,255,0.72)',
    },
    {
      background: 'linear-gradient(165deg, #fff6ec 0%, #fff2e4 50%, #ffedd9 100%)',
      icon: '#c2410c',
      iconBox: 'rgba(255,255,255,0.72)',
    },
    {
      background: 'linear-gradient(165deg, #eefbf3 0%, #e8f7ee 50%, #e3f3e9 100%)',
      icon: '#00b386',
      iconBox: 'rgba(255,255,255,0.72)',
    },
  ];

  const cardShadow =
    'shadow-[0_10px_28px_-8px_rgba(15,23,42,0.14),0_4px_10px_-4px_rgba(15,23,42,0.08)]';
  const cardShadowHover =
    'hover:shadow-[0_22px_44px_-12px_rgba(15,23,42,0.2),0_10px_20px_-8px_rgba(15,23,42,0.12)]';

  const hoverUnderline =
    'transition-[text-decoration,text-decoration-color] duration-300 ' +
    'group-hover:underline group-hover:decoration-[#00b058] group-hover:underline-offset-[3px]';

  const cardClass =
    'group rounded-xl text-left !no-underline hover:!no-underline ' +
    cardShadow +
    ' ' +
    cardShadowHover +
    ' transition-[transform,box-shadow] duration-500 ease-[cubic-bezier(0.22,1,0.36,1)] ' +
    'hover:-translate-y-1.5';

  const cards = [
    {
      title: '1. Build Integration',
      href: 'https://docs.payu.in/docs/collect-payments-with-cards-seamless#1-build-integration',
      icon: 'fa-code',
      text: 'Build your test integration for Merchant Hosted Checkout cards.',
      theme: themes[0],
    },
    {
      title: '2. Test Integration',
      href: 'https://docs.payu.in/docs/collect-payments-with-cards-seamless#2-test-integration',
      icon: 'fa-flask',
      text: 'Validate your Merchant Hosted Checkout integration by testing transactions in the sandbox environment.',
      theme: themes[1],
    },
    {
      title: '3. Production Checklist',
      href: 'https://docs.payu.in/docs/collect-payments-with-cards-seamless#3-go-live-checklist',
      icon: 'fa-check-circle',
      text: 'Follow this checklist to ensure your integration is ready before going live.',
      theme: themes[2],
    },
  ];

  return (
    <div className="my-6">
      <ul
        className="m-0 grid list-none p-0"
        style={{
          display: 'grid',
          gridTemplateColumns: gridTemplateColumns,
          gap: gapPx + 'px',
          alignItems: 'stretch',
          margin: 0,
          padding: 0,
          listStyle: 'none',
        }}
      >
        {cards.map((card) => (
          <li
            key={card.title}
            className="m-0 list-none p-0"
            style={{
              display: 'flex',
              margin: 0,
              padding: 0,
              listStyle: 'none',
              height: '100%',
            }}
          >
            <a
              href={card.href}
              target="_self"
              className={cardClass + ' w-full'}
              style={{
                display: 'grid',
                gridTemplateRows: 'auto auto',
                alignContent: 'start',
                gap: '12px',
                padding: '20px',
                height: '100%',
                width: '100%',
                boxSizing: 'border-box',
                margin: 0,
                textDecoration: 'none',
                background: card.theme.background,
              }}
            >
              <div
                style={{
                  display: 'flex',
                  flexDirection: 'row',
                  alignItems: 'center',
                  gap: '10px',
                  height: '40px',
                  margin: 0,
                  padding: 0,
                }}
              >
                <span
                  className="transition-transform duration-500 group-hover:scale-105"
                  style={{
                    display: 'inline-flex',
                    alignItems: 'center',
                    justifyContent: 'center',
                    width: '40px',
                    height: '40px',
                    minWidth: '40px',
                    borderRadius: '8px',
                    backgroundColor: card.theme.iconBox,
                    color: card.theme.icon,
                    boxShadow: '0 1px 2px rgba(15,23,42,0.06)',
                  }}
                  aria-hidden="true"
                >
                  <i
                    className={'fa ' + card.icon}
                    style={{
                      fontSize: '15px',
                      lineHeight: 1,
                      color: card.theme.icon,
                    }}
                    aria-hidden="true"
                  />
                </span>
                <span
                  className={hoverUnderline}
                  style={{
                    display: 'block',
                    margin: 0,
                    padding: 0,
                    fontSize: '16px',
                    fontWeight: 600,
                    lineHeight: '24px',
                    color: '#0f172a',
                    whiteSpace: 'nowrap',
                  }}
                >
                  {card.title}
                </span>
              </div>
              <span
                className={hoverUnderline}
                style={{
                  margin: 0,
                  padding: 0,
                  fontSize: '14px',
                  lineHeight: '22px',
                  color: '#475569',
                }}
              >
                {card.text}
              </span>
            </a>
          </li>
        ))}
      </ul>
    </div>
  );
};

// Preview - blank line required above this tag

<MerchantHostedCheckoutStepsHoverCards />
